import requestAll from './request.js'
// 本地后台地址
const baseUrl = '';
// 第三方地址 如：高德 腾讯 天气api等接口
const webUrl = '';
// 存放所有请求
const api={
  /*
    请求实例: 
      参数根据接口需要定义
      getBanner(status,test_one){
          // 参数
          let data = {
              status:status
              option:test_one
          }
          return requestAll.requset(baseUrl + '/index/index/volunteerlist', data);
      }, 
  */
}

export default api;
